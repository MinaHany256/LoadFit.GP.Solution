@model IReadOnlyList<VehicleViewModel>
@{
	ViewData["Title"] = "Index";
}

<h1>Index</h1>
<a asp-action="Create" class="btn btn-outline-primary m-2">Add New Vehicle</a>
@if (Model.Count() > 0)
{
	<div class="container">
		<table class="table table-hover table-striped table-bordered dt-responsive nowrap">
			<thead class="bg-dark text-white text-center">
				<tr>
					<th>Id</th>
					<th>Picture</th>
					<th>Name</th>
					<th>Description</th>
					<th>Price</th>
					<th>MaxWeight</th>
					<th>Brand</th>
					<th>Type</th>
					<th>Actions</th>
				</tr>
			</thead>
			<tbody>
				@foreach (var Vehicle in Model)
				{
					<tr>
						<td>@Vehicle.Id</td>
						<td>
							<img class="w-25" src="~/@Vehicle.PictureUrl" />
						</td>
						<td>@Vehicle.Name</td>
						<td>@Vehicle.Description</td>
						<td>@Vehicle.price</td>
						<td>@Vehicle.MaxWeight</td>
						<td>@Vehicle.Brand.Name</td>
						<td>@Vehicle.Type.Name</td>
						<td>
							<div class=" d-flex">
								<a class="btn mb-2 text-warning" asp-action="Edit" asp-route-id="@Vehicle.Id"><i class="fas fa-pen"></i> </a>
								<a class="btn text-danger" asp-action="Delete" asp-route-id="@Vehicle.Id"><i class="fas fa-trash"></i></a>
							</div>
						</td>
					</tr>
				}
			</tbody>
		</table>
	</div>

}
else
{
	<div class="mt-3 p-3 alert-warning">
		<h2>There is no Vehicles!</h2>
	</div>
}
@section Scripts
{
	<partial name="_ValidationScriptsPartial" />
}


